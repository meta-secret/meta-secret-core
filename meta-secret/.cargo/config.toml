[env]
# Integration tests (e.g. meta-secret-tests::test_secret_split) need > default ~2MiB thread stack.
RUST_MIN_STACK = "3145728"
SQLX_OFFLINE = "true"
LIBSQLITE3_SYS_USE_PKG_CONFIG = "0"
SQLITE3_STATIC = "1"
SQLX_SQLITE_BUNDLED = "1"

[build]
rustflags = ["-C", "target-feature=+crt-static"] 